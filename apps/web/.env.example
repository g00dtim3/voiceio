# API URL — the backend URL for Next.js rewrites (server-side proxy)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Set to "true" to enable MSW mock service worker (intercepts API calls with fake data)
NEXT_PUBLIC_MSW_ENABLED=false
